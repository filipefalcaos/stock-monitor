import {
  cilAccountLogout,
  cilBan,
  cilBank,
  cilBell,
  cilCash,
  cilChartPie,
  cilGraph,
  cilSettings,
  cilSync,
  cilTrash,
  cilWallet
} from '@coreui/icons'

export const iconsSet = Object.assign(
  {},
  {
    cilAccountLogout,
    cilBan,
    cilBank,
    cilBell,
    cilCash,
    cilChartPie,
    cilGraph,
    cilSettings,
    cilSync,
    cilTrash,
    cilWallet
  }
)
